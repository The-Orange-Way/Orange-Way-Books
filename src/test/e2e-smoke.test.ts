import { describe, it } from 'vitest';

describe.skip('E2E smoke: onboard → post transaction → verify ledger entries', () => {
  it('TODO: full E2E test requires running Supabase + Playwright', () => {
    // Skipped until local infrastructure is wired up.
  });
});
