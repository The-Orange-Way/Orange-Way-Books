# CODEOWNERS - who gets auto-requested as a reviewer on pull requests.
#
# The branch rulesets on this repo do not require code owner review
# (require_code_owner_review=false on both dev and prod). This file only
# controls who receives automatic review requests. It is deliberately scoped
# to security-sensitive paths where a second set of eyes matters under the
# two-party rule. All other paths produce no automatic request.
#
# Format: <pattern>  <owner> [<owner> ...]

# Cryptography and vault primitives
/src/lib/vault.ts               @MorningRevolution
/src/lib/crypto-fields.ts       @MorningRevolution
/src/context/VaultContext.tsx   @MorningRevolution

# Supabase schema + RLS - migrations are the source of truth.
/supabase/migrations/           @MorningRevolution
/supabase/functions/            @MorningRevolution

# Security disclosure surface.
/SECURITY.md                    @MorningRevolution
